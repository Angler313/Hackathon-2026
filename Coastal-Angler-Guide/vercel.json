{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -r --if-present run build",
  "outputDirectory": "artifacts/fishing-app/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
